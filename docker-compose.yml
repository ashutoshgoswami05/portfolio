services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: ashutosh-portfolio:latest
    container_name: ashutosh-portfolio
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
